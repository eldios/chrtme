"""Utility context for all tests"""

import os, sys
sys.path.insert(0, os.path.abspath(os.path.join('chrtme')))

# Please run the test in tests/../ (parent directory aka project main 
# directory) with make test
import chrtme
import chrtmed
